import { useEffect } from "react";
// TODO: import useDispatch and useSelector from react-redux
// TODO: import fetchUsers from the store

function UserList() {
  // TODO: get dispatch using useDispatch
  // TODO: get users list, loading and error from the Redux store using useSelector

  // TODO: use useEffect to dispatch fetchUsers when the component mounts

  // TODO: if loading is true, return a paragraph that says "Loading..."

  // TODO: if error is not null, return a paragraph showing the error message

  // TODO: render a list of users
  // Each user should display their name and email
  // Use user.id as the key

  return null;
}

export default UserList;
